<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nhnacademy.jdbc.board.domains.reply.mapper.ReplyMapper">
    <select id="selectRepliesByPostId" resultType="com.nhnacademy.jdbc.board.domains.reply.domain.Reply">
        select *
        from BoardReplies
        where bundle_id = #{postId}
    </select>

    <delete id="deleteRepliesByPostId">
        delete
        from BoardReplies
        where bundle_id = #{postId}
    </delete>
</mapper>